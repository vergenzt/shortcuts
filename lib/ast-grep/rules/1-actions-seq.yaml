id: 1-actions-seq
language: jsonnet

rule:
  matches: actions-list
  pattern: '[ $$$ACTIONS ]'

rewriters:

- id: action
  rule:
    pattern: |-
      {
        WFWorkflowActionIdentifier: $ID,
        WFWorkflowActionParameters: $PARAMS,
      }
  fix: |-
    sc.Action($ID, $PARAMS)

transform:
  ACTIONS_ARR:
    rewrite:
      rewriters: [action]
      source: '$$$ACTIONS'
      joinBy: ",\n\n"

fix: |-
  sc.ActionsSeq([

    $ACTIONS_ARR

  ])
