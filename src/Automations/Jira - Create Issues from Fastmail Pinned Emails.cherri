#define color darkgray
#define glyph circlefastforward

const JiraAPIConfig = const JiraAPIConfig = runSelf()
const FastmailAuth = runSelf()
jsonRequest()

for _ in Result {
	const from = 	const from[0] = 	if from[0] {
		getValue(from[0],"name")
	} else {
		getValue(from[0],"email")
	}
	const messageId = getListItem(RepeatItem)
	const CreateIssueResult = formRequest()
	formRequest()
}
